//cfe_idk is not an actual cFS library. It's a play on words.

#include "common_types.h"

typedef uint32_t CFE_RESOURCEID_BASE_TYPE;

// CFE Status type - used for return codes
typedef int32 CFE_Status_t;

// CFE Success return code
#define CFE_SUCCESS ((CFE_Status_t)0)